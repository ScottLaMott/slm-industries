# ~/.tmuxinator/vagrant.yml

name: vagrant
root: ~/ws/vagrant

startup_pane: 1
pre_window: reset

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf
# tmux_options: -v

# Specifies (by name or index) which window will be selected on project startup. If not set, the first window is used.
startup_window: edit-cmd-cmd

windows:
 - edit-cmd-cmd:
    layout: 7793,203x64,0,0[203x47,0,0{101x47,0,0,108,101x47,102,0,113},203x17,0,48,114]
    panes:
     - $EDITOR .
     - clear
     - clear
 - first-box:
    layout: 8fe1,178x55,0,0{116x55,0,0,69,61x55,117,0[61x27,117,0,72,61x27,117,28,73]}
    panes:
     - cd first-box; vagrant status
     - cd test-box; echo "start box ... - - > cmd> vagrant up"
     - cd first-box; clear
 - test-box:
    layout: 8fe1,178x55,0,0{116x55,0,0,69,61x55,117,0[61x27,117,0,72,61x27,117,28,73]}
    panes:
     - cd test-box; vagrant status
     - cd test-box; echo "start box ... - - > cmd> vagrant up"
     - cd test-box; clear
 - slm-box:
    layout: 8fe1,178x55,0,0{116x55,0,0,69,61x55,117,0[61x27,117,0,72,61x27,117,28,73]}
    panes:
     - cd slm-box; vagrant status
     - cd test-box; echo "start box ... - - > cmd> vagrant up"
     - cd slm-box; clear

